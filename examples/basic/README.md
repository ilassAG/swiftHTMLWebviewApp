# Basic Example

Minimal web content that can run inside the wrapper without native payment features.

Use this example to validate:

- WebView boot
- Native bridge availability
- Barcode/document/photo actions if enabled by the host app
